{
  "name": "cookbook-functions",
  "private": true,
  "description": "Milestone 2D: verifies StoreKit purchaseClaims and grants entitlements server-side (Admin SDK), since firestore.rules deliberately never lets a client grant itself an entitlement increase.",
  "type": "commonjs",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "test": "node --test --test-concurrency=1"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2",
    "@apple/app-store-server-library": "^3.1.0"
  }
}
