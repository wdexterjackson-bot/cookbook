# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/

# Firebase config — contains API keys, per-developer/per-environment
cookbook/GoogleService-Info.plist

# Third-party API keys (Spoonacular, etc.) — per-developer, never committed
cookbook/Secrets.plist

# macOS
.DS_Store

# Firebase emulator (rules-tests)
firestore-debug.log
firebase-debug.log
.firebase/
rules-tests/node_modules/
functions/node_modules/
storage-tests/node_modules/

# Apple root certs for App Store Server Library verification — fetched
# manually per functions/certs/README.md, not committed.
functions/certs/*.cer
